<Window x:Class="Task7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task7"
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="300">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="FontFamily" Value="Dubai Light"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Rectangle x:Name="checkBoxRectangle" Width="16" Height="16" Stroke="Black" RadiusX="3" RadiusY="3" Grid.Row="0"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="1"/>
                            <TextBlock x:Name="x" Text="X" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" Opacity="0"/>
                            <Ellipse x:Name="ellipse" Width="8" Height="8" Grid.Row="0" Fill="Black" Opacity="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="x" Property="Opacity" Value="1"/>
                                <Setter TargetName="ellipse" Property="Opacity" Value="0"/>
                                <Setter Property="Content" Value="Checked"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="x" Property="Opacity" Value="0"/>
                                <Setter TargetName="ellipse" Property="Opacity" Value="1"/>
                                <Setter Property="Content" Value="Indeterminate"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="x" Property="Opacity" Value="0"/>
                                <Setter TargetName="ellipse" Property="Opacity" Value="0"/>                                
                                <Setter Property="Content" Value="Unchecked"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <CheckBox IsChecked="True" Grid.Column="0"/>
        <CheckBox IsChecked="False" Grid.Column="1"/>
        <CheckBox IsChecked="{x:Null}" Grid.Column="2"/>
    </Grid>
</Window>
