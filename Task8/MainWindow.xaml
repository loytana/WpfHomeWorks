<Window x:Class="Task8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task8"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="ProductDataTemplate">
            <Border x:Name="border" Padding="10" BorderBrush="Blue" BorderThickness="1" CornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" Width="100" Height="100"
                           Source="{Binding Image}" />
                    <TextBlock x:Name="nameValue" Grid.Row="0" Grid.Column="1" Text="{Binding Name}" Margin="70,0,0,0"/>
                    <TextBlock x:Name="priceValue" Grid.Row="1" Grid.Column="1" Text="{Binding Price}" Margin="70,0,0,0"/>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Category}">
                    <DataTrigger.Value>
                        <local:Categories>Food</local:Categories>
                    </DataTrigger.Value>
                    <Setter TargetName="border" Property="BorderBrush" Value="Green" />
                    <Setter TargetName="border" Property="Background" Value="#E8F5E9" />
                    <Setter TargetName="nameValue" Property="Foreground" Value="Green"/>
                    <Setter TargetName="nameValue" Property="FontWeight" Value="Bold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Category}">
                    <DataTrigger.Value>
                        <local:Categories>Appliances</local:Categories>
                    </DataTrigger.Value>
                    <Setter TargetName="border" Property="BorderBrush" Value="Orange" />
                    <Setter TargetName="border" Property="Background" Value="#FFF3E0" />
                    <Setter TargetName="nameValue" Property="Foreground" Value="Blue"/>
                    <Setter TargetName="nameValue" Property="FontWeight" Value="Bold"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>
    <StackPanel>
        <TextBlock HorizontalAlignment="Center" FontSize="20" Text="Список товаров:" />
        <ListBox Name="productListBox" Width="400" Margin="10" HorizontalContentAlignment="Stretch"
                 ItemTemplate="{StaticResource ProductDataTemplate}" >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button Margin="10" Click="Button_Click" Content="Добавить товар" />
    </StackPanel>
</Window>
